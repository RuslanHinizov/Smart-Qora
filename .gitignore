.env
.env.local
backend/.env
frontend/.env
__pycache__/
.pytest_cache/
.ruff_cache/
*.py[cod]
node_modules/
dist/
*.tsbuildinfo
vite.config.js
vite.config.d.ts
# Weights are not committed — except the bundled demo model, so the repo runs
# straight after `git clone` (see models/README.md).
models/*.pt
models/*.ts
models/*.pth
!models/best.pt
/yolo*.pt
# Videos are not committed (any depth) — except the one bundled demo clip.
videos/**/*.mp4
videos/**/*.MP4
videos/**/*.xlsx
!videos/crop_23.11.23-12.MP4
datasets/_downloads/
datasets/**/images/
datasets/**/labels/
datasets/**/_tmp_video/
runs/
training/reports/
coverage/
*.db
*.log
.codebase-memory/
.claude/
