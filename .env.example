# Root .env — variables consumed by docker-compose.yml.
# App/model configuration lives in backend/.env (see backend/.env.example).

POSTGRES_PASSWORD=change-me
APP_ENV=production
SECRET_KEY=change-me-to-a-long-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
COOKIE_SECURE=false
TZ=Asia/Almaty
